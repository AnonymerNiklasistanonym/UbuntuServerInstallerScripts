#!/usr/bin/env bash

# -----------------------------------------------------
# | INSTALLER SCRIPT: astyle
# -----------------------------------------------------

# Install it via the existing ubuntu package manager
sudo apt-get update
sudo apt-get install astyle
